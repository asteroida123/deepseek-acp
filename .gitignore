# 开发阶段材料
/.docs/
/reference/
/spikes/
/CHANGELOG.md
/RELEASING.md

# 构建产物
/lib/
*.tsbuildinfo

# 依赖
node_modules/

# 打包产物
*.tgz

# 覆盖率
/coverage/

# 会话日志
.sessions/

# 用例在仓库根下造的临时工作区（`tests/sandbox.spec.ts` 要一个工作区**之外**
# 但仍在磁盘上的目录）。用例正常结束会清掉；崩在中间时别让它污染 git status。
.dsacp-*

# 凭据
.env
.env.*
!.env.example
.credentials.yaml
.credentials.yml
*.pem

# 编辑器与系统
.DS_Store
.vscode/
.idea/
*.swp
.claude/settings.local.json
